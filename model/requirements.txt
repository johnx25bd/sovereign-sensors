# Pinned to versions contemporary with the dissertation (mid-2019,
# Python 3.6), so the committed pickles and notebooks load as written.
mesa==0.8.5
numpy==1.16.6
pandas==0.25.3
scipy==1.3.3
matplotlib==3.1.3
seaborn==0.9.1
statsmodels==0.10.2
notebook==5.7.8
